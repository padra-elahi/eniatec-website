# Optional: a form backend that accepts a JSON POST (Formspree, Getform,
# a Cloudflare Worker, …). When unset, the contact form falls back to opening
# the visitor's mail client with the message pre-filled.
# NEXT_PUBLIC_CONTACT_ENDPOINT=https://formspree.io/f/xxxxxxx
